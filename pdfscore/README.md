# pdfscore 

Generate multiple exports from MuseScore.

Example:

```
pdfscore --key C,Bb myscore.mscz
```
will generate `myscore.pdf` for concert key and `myscore.Bb.pdf` for Bb key.

More information:

```
pdfscore --help
```
